{
  "name": "@mashupkgrid/billing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mashupkgrid/config": "workspace:*",
    "@mashupkgrid/database": "workspace:*",
    "@mashupkgrid/radius": "workspace:*",
    "@mashupkgrid/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
